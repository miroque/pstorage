# Запускать dockerize.sh из корня проекта

примеры:

`./docker/dockerize.sh build     # собрать образы`

`./docker/dockerize.sh clean     # удалить локальные образы`

`./docker/dockerize.sh push      # запушить образы в репку`
